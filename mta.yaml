_schema-version: "3.2"
ID: reuseuidms
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: reuseuidms-destination-content
  type: com.sap.application.content
  requires:
  - name: reuseuidms-destination-service
    parameters:
      content-target: true
  - name: reuseuidms-repo-host
    parameters:
      service-key:
        name: reuseuidms-repo-host-key
  - name: reuseuidms-uaa
    parameters:
      service-key:
        name: reuseuidms-uaa-key
  - name: dms-repo
    properties:
      destinations: "[{\"name\": \"sap_dms_repo_backend\", \"url\": \"https://api-sdm-di.cfapps.us10.hana.ondemand.com\", \"forwardAuthToken\": true}]"
  parameters:
    content:
      subaccount:
        destinations:
        - Name: reuseuidms_html_repo_host
          ServiceInstanceName: reuseuidms-html5-srv
          ServiceKeyName: reuseuidms-repo-host-key
          sap.cloud.service: reuseuidms
        - Authentication: OAuth2UserTokenExchange
          Name: reuseuidms_uaa
          ServiceInstanceName: reuseuidms-xsuaa-srv
          ServiceKeyName: reuseuidms-uaa-key
          sap.cloud.service: reuseuidms
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: reuseuidms-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: reuseuidms-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - reuseuidms.zip
      name: reuseuidms
      target-path: resources/
- name: reuseuidms
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: reuseuidms-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: reuseuidms-destination-service
    service-plan: lite
- name: reuseuidms-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: reuseuidms-xsuaa-srv
    service-plan: application
- name: reuseuidms-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: reuseuidms-html5-srv
    service-plan: app-host
- name: dms-repo
  type: org.cloudfoundry.managed-service
  parameters:
        service: sdm
        service-plan: free  
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
